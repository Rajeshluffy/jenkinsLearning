apiVersion: batch/v1
kind: Job
metadata:
  name: sdet-test-job
spec:
  template:
    spec:
      containers:
      - name: sdet-test
        image: sdet-test:latest
        imagePullPolicy: Never
      restartPolicy: Never
  backoffLimit: 0